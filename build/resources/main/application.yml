spring:
  datasource:
    username: teinejo
    password: tg3055tp3055
    url: jdbc:postgresql://localhost:5432/testDB
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQL10Dialect

